declare_args() {
  # mds config
}

declare_toolchain(profile_toolchain) {
}

group("kernel") {
  public_deps = [ "kernel/:mds_kernel" ]
}

group("device") {
  public_deps = [ "device/:mds_device" ]
}

group("default") {
  public_deps = [
    # ":device",
    ":kernel",
  ]
}
